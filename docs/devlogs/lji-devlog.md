# lji Devlog

## 2026-07-15

- 한 일:
  - `Project3_Sherpa` 잘못 클론했던 것 확인 → `Project3_AIReviewBoard`로 재클론, `dev` 기준 `lji` 브랜치 생성/push
  - Hugging Face에서 MuseTalk v1.5 모델 가중치 다운로드 후 Google Drive 업로드
  - `ai/media/musetalk/colab/musetalk_setup.ipynb` 작성 — Colab A100에서 MuseTalk 환경 구축부터 샘플 추론까지 자동화
  - 여러 설치 오류 해결 후 샘플 추론 성공
  - 팀 코드 구조 확인 — `contracts/`의 `mediaLine` 입력 스키마 파악. 영상 결과 반환 계약은 아직 없음
  - 페르소나 A 후보 영상(AI 생성, `avata_rf.mp4`) 확보 → 루프 이음매·정지도 점검 통과
  - edge-tts로 실제 위원 발언 문장을 TTS 생성해 페르소나 A 영상에 립싱크 적용, 파이프라인 동작 확인
  - 우글거림(jitter) 대응: extra_margin 조정, 입 주변 색보정, 프레임 시간축 스무딩 적용
  - 발화 종료 후 립싱크 결과를 무음 tail(원본 루프)로 이어붙여 길이를 맞추는 로직 여러 차례 개선 (자세한 내용은 "막힌 점" 참고) — 완전히 매끄럽진 않으나 동작은 함, 품질은 보류하고 다음 단계로 넘어가기로 함
  - 노트북 11~14번 섹션 추가: FastAPI로 파이프라인을 감싸서(`/generate`, mediaLine 스키마 기반 요청) cloudflared Quick Tunnel로 외부 노출, 실제 HTTP 요청으로 E2E(문서→API→영상) 테스트까지 구성
  - 작업분해표(`AI_Review_Board_HAI_직접구축_작업분해.xlsx`)에 진행상황 반영
  - 3단계(TTS 연동) 마무리: `ai/media/tts/tts_engine_decision.md`(엔진 비교/선정), `ai/media/tts/voice_map.json`(위원 A/B 보이스 매핑), `ai/media/tts/audio_duration.py` + 단위테스트 작성
  - TTS 엔진을 처음엔 edge-tts로 혼자 정했다가, 재인님 피드백으로 다시 논의 → OpenAI TTS(`gpt-4o-mini-tts`)로 최종 확정
  - 노트북 7번(TTS 생성), 11번(FastAPI `app.py`) 전부 edge-tts → OpenAI TTS로 교체, 정상 동작 검증 완료
  - `/generate-stream` WebSocket 엔드포인트 추가 — 완성본을 기다리지 않고 MuseTalk이 프레임을 만드는 대로 청크 단위로 인코딩해서 전송하는 구조로 확장, `web_test_stream.html`로 브라우저 재생 테스트
  - 스트리밍 첫 응답 지연이 커서 원인 진단 및 최적화 진행 (배치 인터리브, 페르소나 준비 캐싱, 지연 import, 모델 상주 구조로 `app.py` 리팩터링) — Colab에서 실제 재기동해 개선 효과까지 검증 완료. 정량적 진단 과정과 개선 수치는 `ai/media/musetalk/streaming_optimization.md`에 별도 기록
  - 스트리밍 재생이 청크 경계마다 끊기는 문제 확인 → `web_test_stream_mse.html`(신규) + 백엔드를 MediaSource 기반 연속 스트리밍 구조로 전환. 이 과정에서 ffmpeg가 패킷을 즉시 흘려보내지 않는 문제, 이어서 GPU 추론 스레드와 asyncio 이벤트 루프가 GIL을 두고 경합해 전송이 들쭉날쭉해지는 문제를 차례로 발견해 해결
  - 위 작업 중 근본적인 문제를 하나 더 발견: 지금 구조의 실제 프레임 생성 속도가 재생 속도보다 훨씬 느려서(대략 1/3배속), 스트리밍 구조를 아무리 다듬어도 재생 중 버퍼링이 남음 — 2차 프로젝트가 겪었던 것과 같은 종류의 CPU 블렌딩 병목으로 추정, 다음 작업으로 `_blend_fast` 스타일 최적화 포팅 예정
  - 배치 크기를 8→16으로 늘려봤지만 프레임당 처리 시간이 그대로라 GPU 배칭 문제가 아니라는 걸 확인 → MuseTalk 실제 소스코드를 직접 받아서 확인해보니 매 프레임마다 얼굴 파싱 신경망을 다시 돌리고 있었던 게 원인. 얼굴 위치가 고정인 페르소나 특성상 마스크는 한 번만 계산해도 되는 걸 활용해 프레임별 마스크를 페르소나 준비 단계에서 미리 계산해 캐싱하도록 변경
  - 마스크 캐싱 이후에도 남아있던 PIL 이미지 변환 왕복(매 프레임 풀 프레임 크기로 변환)을 2차 프로젝트 방식을 참고해 numpy 기반으로 새로 작성해 제거, 프레임을 ffmpeg로 넘길 때도 불필요했던 PNG 압축/해제 왕복을 없애고 압축 없는 원본 바이트를 바로 넘기도록 변경 — 이 두 가지로 생성 속도가 실시간보다 빨라짐(세션 내 가장 큰 개선)
  - 생성 속도가 실시간보다 빨라졌는데도 재생은 여전히 끊겨서 원인을 더 보니, GPU가 배치 단위로 몰아서 프레임을 만들다 보니 평균은 빨라도 순간순간 전달이 고르지 않은 게 문제였음 → 프레임을 만드는 속도와 ffmpeg로 전달하는 속도를 분리해서, 전달은 별도 스레드가 일정한 간격으로 하도록 구조 변경
  - 다만 이 페이싱을 재생 속도와 똑같은 속도로 맞춰버려서, 모처럼 확보한 "생성이 재생보다 빠른" 여유분을 그대로 버리는 바람에 재생 중 버퍼링이 일정한 주기로 계속 반복되는 새로운 문제 발견 — 2차 프로젝트는 애초에 생성이 재생보다 느린 상황이라 이런 페이싱 자체를 안 썼다는 걸 다시 확인
  - 페이싱 배속을 재생보다 살짝 빠르게(1.5배) 조정해봤는데도 버퍼링 패턴이 거의 그대로라서 이상하다 싶어 전송 구간에 타이밍 계측을 추가해봄 → 서버 프로세스 안에서는 전송이 전혀 느리지 않다는 게 확인돼서, 병목이 우리 코드가 아니라 지금 쓰고 있는 Cloudflare Quick Tunnel(무료, 계정 없는 임시 터널이라 대역폭 보장이 없음)의 실제 네트워크 구간이라는 걸 확정함 — 인코딩 비트레이트를 낮춰서 재현 테스트했더니 버퍼링이 완전히 사라져서 가설 검증 완료
  - Named Cloudflare Tunnel(도메인 필요) · ngrok 같은 대안도 검토했지만 지금 단계에서 무료로 안정적인 도메인/터널을 구하기가 마땅치 않아서, 일단 Quick Tunnel을 그대로 쓰고 화질(해상도·비트레이트)을 낮추는 쪽으로 대응하기로 함 — 화질을 단계적으로 올려가며 버퍼링 안 생기는 절충점을 찾아 확정, 프론트엔드 버퍼 임계값도 같이 재조정해서 초반 대기 시간도 줄임
  - 위 작업 중 프론트엔드 테스트 페이지의 부수적인 버그 두 개 발견해 수정: 새로고침 없이 반복 테스트하면 비디오 이벤트 리스너가 계속 누적 등록되던 문제, `MediaSource` 상태 관련 간헐적 에러 로그
  - 이걸로 스트리밍 성능 최적화 작업은 일단 마무리 — 최종 수치와 설정값은 `ai/media/musetalk/streaming_optimization.md`에 정리
  - 예전에 실험만 해두고 API에는 안 붙였던 "무음 tail(원본 루프) 이어붙이기" 로직을 실시간 스트리밍(`/generate-stream`)에도 포팅 — 발화 길이와 원본 루프 자체 길이를 기준으로 루프 길이의 배수가 되도록 나머지 구간을 원본 루프 프레임 그대로 이어붙여서, 발화가 끝나면 정확히 루프의 끝 지점에서 마무리되게 함(MuseTalk 추론 없이 원본 프레임만 흘려보내서 추가 비용 거의 없음)
  - 위원과 실제로 대화하듯 테스트해볼 수 있는 페이지(`web_test_chat.html`, 신규) 제작 — 평소엔 위원 대기 루프 영상이 계속 재생되다가, 메시지를 보내면 그 자리에서 발언 스트림으로 전환되고 끝나면 다시 대기 루프로 자연스럽게 복귀하는 구조. 서버에서 원본 루프 영상 파일을 내려줄 수 있는 정적 파일 경로(`/media`)도 새로 추가
  - 초기 버전에서 대기 루프가 자동으로 시작이 안 되는 문제가 있어서, 서버 주소 입력 후 명시적으로 누르는 "연결" 버튼을 추가하고 영상 에러 발생 시 원인을 화면에 바로 표시하도록 개선
  - 화면 디자인을 화상회의 앱(줌 등) 느낌으로 다시 작업 — 영상 타일에 화자명/상태를 배지로 오버레이하고, 채팅 로그를 아바타 아이콘 + 말풍선 형태로 정리
- 결정/이유:
  - Colab 기본 Python(3.12)이 `mmcv==2.0.1` wheel을 지원 안 해서 Miniconda로 Python 3.10 가상환경(`musetalk`) 분리
  - 버전 조합(torch, mmcv 등)은 2차 프로젝트에서 검증된 조합 재사용
  - MuseTalk v1.0 대신 공식 권장 버전인 **v1.5**로 확정
  - 오디오를 무음으로 통째로 패딩하는 방식은 MuseTalk이 무음 구간에서도 입을 계속 움직여서 폐기 → TTS 구간만 립싱크 생성 후 원본 루프 영상을 이어붙이는 방식으로 전환
  - API 1차 버전은 색보정/스무딩/길이 패딩 같은 품질 후처리를 전부 빼고 TTS+립싱크만 연결 — 품질보다 "문서→API→영상" 배관이 실제로 되는지 먼저 검증하는 게 우선이라고 판단
  - TTS 엔진처럼 팀 문서에 남는 결정은 혼자 정하지 말고 먼저 물어봐야 한다는 걸 재인님 피드백으로 확인함 — 앞으로 이런 선택지는 옵션을 제시하고 확인받은 뒤 문서화하기로 함
  - 스트리밍 성능 최적화는 요청마다 새 프로세스를 띄우는 구조 자체가 근본 원인이라고 진단, subprocess 방식을 버리고 서버가 모델을 한 번만 로드해 상주시키는 구조로 전환하기로 결정
- 막힌 점:
  - `mim install`이 Python 3.12 비호환 문제로 반복 실패 → 3.10 가상환경으로 해결
  - `conda create` 시 Anaconda ToS 미동의 에러 → `conda tos accept`로 해결
  - `openmim`이 `openxlab` 의존성 때문에 setuptools를 도로 낮추는 문제 → 설치 순서 조정으로 대응
  - "런타임 재시작" 경고 뜨자마자 눌러서 셀 뒷부분이 안 끝나고 끊기는 실수 반복 → 셀 분리 + 안내 문구 추가
  - `mmpose` 의존 패키지 `chumpy` 빌드 실패 → `--no-build-isolation`으로 해결
  - 샘플 추론 시 `matplotlib`이 Colab 기본 커널의 `MPLBACKEND`를 상속받아 에러 → `MPLBACKEND=Agg`로 해결
  - 립싱크→무음 tail 이음매에서 자세는 맞춰도 **입모양 자체**가 달라 보이는 문제 → 여러 방식(위치 계산, 입모양 최근접 매칭)을 시도했으나 완전히 해결되지 않음 — 프로토타입 단계에서는 보류, 나중에 재작업 필요
  - 발화 종료 후에도 입이 잠깐 더 움직이는 문제 → 오디오 패딩 조정 + 발화 종료 시점 감지 + 페이드아웃으로 대응, 완전히는 아니어도 크게 완화됨
  - fps를 하드코딩했던 실수 발견 (페르소나 A 영상 실제 fps와 불일치) → 파일에서 동적으로 fps를 읽어오도록 수정
  - `xfade`/`acrossfade` concat이 멈추는 현상 → 두 클립의 오디오 포맷 불일치가 원인으로 추정, 동일 스펙으로 정규화한 뒤 처리해 해결
  - 결과 영상이 윈도우 기본 플레이어에서 재생 안 되는 문제 → 출력 코덱 명시적 지정으로 해결
  - `/generate-stream` 첫 응답까지 지연이 심했던 문제 → 원인을 여러 차례 재진단한 끝에 "요청마다 모델을 새로 로드하는 구조" 자체가 근본 원인임을 확인, 진단 과정과 각 시도의 결과는 `ai/media/musetalk/streaming_optimization.md` 참고
- 다음 할 일:
  - `web_test_chat.html`로 여러 발언을 연달아 보내보면서 대화형으로 어색한 부분 없는지 확인 (영상 더 뽑아보면서 계속 확인 예정)
  - 강사님이 만들어둔 API를 우리 프로젝트에 연결 (새 대화로 진행 예정)
  - 위원 B(`cedar`) 스타일 프롬프트 확정
  - 페르소나 B 영상 확보 및 이음매·정지도 검증
  - 발화→무음 tail 경계에서 배경 자세가 기존 ping-pong 방식과 tail의 시간 기준 계산 방식이 달라 미세하게 튈 수 있음 — 알려진 한계, 눈에 띄면 추가 개선
  - `contracts/`에 영상 생성 결과 반환 규격(video_url, 상태값 등) 제안 — 아직 계약에 없음

## 2026-07-16

- 한 일:
  - `/generate-stream` 스트리밍이 코랩 재기동 후에도 여전히 잘 되는지 재검증한 뒤, 지금까지 독립 테스트 페이지(`web_test_chat.html` 등)로만 검증했던 파이프라인을 실제 서비스(AI Review Board 백엔드/프론트) 화면에 처음으로 연동
  - 작업 시작 전 팀 규칙 재확인 — `ai/media/` 밖(`backend/`, `frontend/`, `contracts/`) 파일을 건드릴 때마다 매번 허락을 받고, 파일마다 신규/추가/수정 여부를 라벨로 구분해서 보여주는 방식으로 진행하기로 함
  - 아키텍처를 "Job ID + 상태 폴링" 대신 실시간 WebSocket 중계(백엔드가 프론트↔코랩 사이를 그대로 릴레이) 방식으로 확정, 통신 규격 문서(`contracts/schemas/media_stream.schema.md`, 신규, 윤한님 확인 필요한 DRAFT로 명시)를 먼저 작성
  - 백엔드에 위원 발언 영상 스트리밍 중계 라우터(`backend/app/api/routes/media.py`, 신규) 작성 — `/media/available-speakers`(아바타 준비된 위원 목록), `/media/stream`(코랩으로 그대로 중계). `MEDIA_SERVICE_WS_URL` 환경변수를 `config.py`/`.env.example`에 추가하고 `main.py`에 라우터 등록
  - conda 환경(`review-board`)을 새로 구성해 백엔드를 로컬에서 띄우고, 윤한님 홈서버로 SSH 터널을 연결해 실제 MongoDB로 테스트 진행
  - 프론트에 위원 발언 영상 재생 컴포넌트(`frontend/src/components/meeting/CommitteeVideoStage.jsx`, 신규) 작성 — `web_test_chat.html`에서 검증한 스트리밍/버퍼 관리 로직을 React로 이식, `ProjectDetailPage.jsx` 화면 상단(기존 텍스트 채팅 위)에 배치. 아바타 영상이 준비된 위원(현재 1명)만 자동 재생하고 나머지는 기존 텍스트로만 보여주는 흐름으로 설계
  - 경이님 파일(`ai/meeting/graph/run.py`)을 확인하다가 회의 결과의 `media_script`가 항상 빈 배열로 하드코딩돼 있는 걸 발견 → 허락받고, 새 LLM 호출 없이 기존 `reviewer_results`를 재포장해서 채우는 함수 추가. DB 저장 쪽(`backend/app/api/routes/meetings.py`)에도 같은 하드코딩이 별도로 있던 걸 발견해 같이 수정
  - 실제 브라우저 테스트 중 오디오 디코더 에러(`DECODER_ERROR_NOT_SUPPORTED`)로 영상은 나오는데 소리가 안 나오는 문제를 겪음 — 백엔드 중계 우회 테스트, 동일 텍스트 A/B 테스트, React 리렌더/`muted` 속성 가설 순서로 하나씩 원인을 배제해나감
  - 최종적으로 코드 문제가 아니라 VSCode 내장 브라우저(웹뷰)에서 테스트하고 있었던 게 원인으로 확인됨 — 실제 크롬 창에서는 정상 재생
  - 실제 크롬에서도 오디오가 재생되다가 중간에 끊기고 재시작되는 문제를 추가로 발견, 로그 분석 끝에 가은님 파일(`ProjectDetailPage.jsx`)의 `useEffect`가 (React StrictMode 때문에) 회의 분석(`/analyze`) 요청 자체를 실제로 두 번 보내고 있었던 게 원인임을 확인 → 허락받고 요청 중복 방지 가드 추가
  - 오늘 새로 만들거나 수정한 파일 전부에 "재인/Claude가 왜, 무엇을, 어디와 연결해서 만들었는지" 주석 추가. 디버깅 중 넣었던 진단용 로그도 실제 재생 확인 후 정리
  - `.gitignore`에 스크래치 테스트 파일(`web_test_stream_mse.html`)·팀 작업분해표(xlsx) 제외 규칙 추가, 오늘 변경분 전부 커밋 후 `lji` 브랜치에 push 완료(중간에 `dev` 최신 커밋을 병합해서 받아옴)
  - 강사님이 "서버 하나로 위원 여러 명(2~3명)을 각자 다른 얼굴/목소리로 처리하는 게 실제로 되냐"고 확인을 요청하셔서, 검증용 노트북(`musetalk_setup_two_avatars_test.ipynb`, 신규)과 검증용 HTML(`web_test_chat_two_avatars.html`, 신규 — 위원 A/B 입력창을 완전히 분리한 버전)을 만들어 실제로 두 위원이 한 서버에서 섞이지 않고 처리되는지 확인
  - 검증 과정에서 3단계로 문제를 겪음: ①새 코랩 세션이라 페르소나 A 영상 파일 자체가 없어서 서버가 아예 안 뜸 ②페르소나 B 영상 파일명이 코드(`avata_rf_b.mp4`)와 실제 업로드 파일(`avata_b_rf.mp4`)이 달라서 또 안 뜸 ③파일명을 고쳐도 계속 "unknown speaker_id" 에러가 나서 봤더니, `%%writefile app.py` 셀은 다시 실행해야만 코랩 디스크의 실제 파일이 바뀐다는 걸 몰라서 옛날 버전이 계속 떠 있었음 — 이 세 가지를 순서대로 해결
  - 최종적으로 uvicorn 로그 + 브라우저 디버그 로그로 검증 완료: 위원 A/B 요청이 순서대로 와도, 심지어 시간이 겹쳐서 와도 각자 정확한 얼굴/목소리로 섞이지 않고 처리됨을 확인(겹친 요청도 각각 8~9초 만에 정상 완료)
  - TTS 지연(5.8~6.9초) 문제를 A100 GPU를 활용해 로컬 TTS로 해결할 수 있는지 조사 — MeloTTS/XTTS-v2/CosyVoice2/Zonos 등 비교, 실사용 후기 기준으로는 공식 벤치마크 1위였던 CosyVoice2가 오히려 "기계음이 제일 심하다"는 평가였고 Zonos가 한국어 자연스러움 1위였음(단, Zonos는 한국어가 공식 지원 언어 목록엔 없어 직접 검증 필요). 비교 테스트용 노트북(`musetalk_setup_zonos_test.ipynb`, 신규, 별도 conda 환경으로 분리해서 `musetalk` 환경 안 건드리게 구성) 작성
  - Cloudflare Quick Tunnel의 버퍼링 문제를 Named Tunnel로 해결할 수 있는지 조사 — 터널 서비스 자체는 여전히 무료이고 도메인만 있으면 되는데, 로그인만으로 되는 게 아니라 도메인을 Cloudflare DNS에 미리 연결해야 하는 게 핵심 전제조건임을 확인. 정확한 대역폭 비교 수치는 Cloudflare가 공개한 자료가 없어서 "몇 배 빠르다"고 단정할 근거는 없음(정성적으로 "테스트용 vs 프로덕션용" 차이만 문서화돼 있음)
  - 우글거림 대응(색보정+시간축 스무딩) 코드를 다시 점검하다가, 이 실험이 8번 섹션(`scripts.inference` CLI 경로)의 결과물에 의존하는데 그 경로가 `os.system()`을 써서 실패해도 조용히 넘어가는 버그가 있었던 걸(지난 세션에 진단) 다시 확인 — 즉 색보정 실험이 애초에 제대로 된 입력으로 돌았는지 불확실하고, 애초에 지금 실제 서비스가 쓰는 경로(`app.py`의 `run_inference_sync`)와는 아예 다른 코드라 반영도 안 돼 있었다는 걸 재확인. 위원 A 것부터 실제 서비스 경로 결과물로 다시 검증하기로 함(다음 세션에서 진행)
  - 경이님이 회람한 계약 개정 제안(`review_output.v2.1.proposal.md`, RAG-006 유사사례를 선택 필드 `similar_success_cases`로 추가하는 v2.1.0 개정) 검토 — 재인 담당(`ai/media/`) 코드는 `media_script`만 쓰고 `schema_version`을 참조하지 않아 영향 없음을 확인. 다만 코드 전체를 찾아보니 `schema_version: "2.0.0"` 고정값이 `ai/meeting/graph/run.py`, `backend/app/api/routes/meetings.py`, `backend/app/models/meeting.py` 3군데 있는 걸 발견해 경이님께 회신할 내용에 포함
  - 남은 작업 3개(제스처 영상 여러 개 / TTS 대안 / 오디오-입모양 싱크 드리프트 검증)의 순서를 정함 — 제스처 영상이나 TTS가 바뀌면 싱크 결과도 같이 바뀔 수 있어서, 싱크 드리프트 검증은 앞의 둘이 끝난 뒤 마지막에 하기로 함
  - 페르소나 A/B 제스처 영상(발화용, 이미지-투-비디오 AI로 생성) 프롬프트를 두 번 수정 — 1차로 "종이 보며 필기하는 동작"을 넣었다가 시선이 카메라를 벗어나 거의 안 보이는 결과가 나와서, MuseTalk이 코 부리~턱 랜드마크로 크롭 영역을 계산한다는 걸 근거로 필기 동작을 완전히 빼고 시선을 카메라에 고정하도록 수정(공식 `BBOX_SHIFT.md` 문서로 근거 확인). 근데 이번엔 "머리 완전 고정" 지시가 너무 세게 들어가서 호흡 등 자연스러운 움직임까지 같이 죽어 정지사진처럼 나옴 → 시선·고개 방향(고정 유지)과 몸의 자연스러움(호흡·손동작, 강조)을 분리해서 재작성, 여성(페르소나 A)·남성(페르소나 B) 버전 각각 만듦
  - 옛날 실험 섹션(6-1/8/9/10번)을 뺀 새 노트북(`musetalk_setup_v2.ipynb`, 신규)을 만들어 실제 서비스 경로(`app.py`) 기준으로 다음 작업을 이어가기로 함
  - 위원 1명당 제스처 영상 여러 개 지원 메커니즘 구현 — `PERSONA_MAP`을 `video_path`(문자열 1개)에서 `video_paths`(리스트)로 바꾸고, 요청에 `gesture_index`(0/1/2)를 받아 그중 하나로 립싱크하도록 `/generate`·`/generate-stream` 수정. 서버 시작 시 리스트 안 영상 전부 캐싱, 범위 밖 인덱스는 조용히 폴백하지 않고 400 에러로 명확히 처리. "나중에 영상이 더 늘어날 수도 있다"는 요구사항 때문에 개수 고정 없이 리스트 길이에 맞춰 동작하도록 설계
  - 재인님이 위원 A/B 각각 제스처 영상 2개씩(`avata_1/2.mp4`, `avata_b_1/2.mp4`) 실제로 준비, 코랩에서 `musetalk_setup_v2.ipynb` 실행해 검증 — `/health`로 위원당 영상 3개(대기 루프 포함)씩 총 6개 캐싱 확인, `gesture_index=0/1/2` 각각 실제로 다른 영상으로 정상 립싱크됨을 확인, 범위 밖 인덱스(99) 요청 시 예상대로 400 에러 확인. 위원 1명당 제스처 영상 여러 개 지원, 메커니즘 검증 완료
- 결정/이유:
  - 아키텍처는 폴링 대신 실시간 WebSocket 중계(B안)로 확정 — 배달 앱 비유로 두 방식을 설명한 뒤 재인님이 직접 선택
  - 아바타가 준비 안 된 위원의 폴백 방식을 기존 팀 결정(정적 이미지+TTS, `docs/03_DECISIONS.md`)에서 "무한루프 영상+TTS"로 바꿔서 우선 구현 — 팀 동의가 필요한 임시 변경임을 문서에 명시해둠
  - `media_script`는 위원 전원을 다 채우고 "실제 아바타 영상을 만들지 말지"는 프론트(`CommitteeVideoStage`)에서만 걸러내기로 함 — 처음엔 백엔드에서 아바타 있는 위원만 걸러서 채웠다가, 텍스트 채팅(`MeetingChat.jsx`)까지 같이 줄어드는 부작용을 발견해서 되돌림
  - 다른 담당자 파일(경이님, 가은님)을 건드릴 때마다 파일 단위로 미리 허락을 받고 진행 — 회의 분석 파이프라인과 화면 흐름을 잘 아는 담당자 확인 없이는 구조를 바꾸지 않기로 함
  - 위원 B 검증은 별도 테스트 노트북/HTML로 먼저 증거를 만들고, 실제 서비스(`PERSONA_MAP`, `AVAILABLE_SPEAKER_IDS`) 반영은 검증 끝난 뒤 별도로 진행하기로 함 — 검증과 실서비스 반영을 분리
  - TTS 엔진을 로컬 모델(Zonos 등)로 바꾸는 건 비교 테스트까지만 준비하고 실제 전환 여부는 팀과 논의 후 결정하기로 함 — edge-tts→OpenAI TTS 때처럼 엔진 선택은 팀 결정 사항이라 임의로 안 바꿈
  - 제스처 영상 선택 로직(`gesture_index`)은 지금 단계에서 "발언 내용 보고 자동으로 고르는" 것까지 안 만들고, 요청 쪽에서 직접 인덱스를 지정하는 수준까지만 구현 — 메커니즘(영상 여러 개를 등록·캐싱·선택하는 구조)과 선택 로직(무엇을 기준으로 고를지)을 분리해서, 메커니즘부터 확실히 검증하기로 함
- 막힌 점:
  - 영상은 재생되는데 오디오만 디코더 에러로 계속 실패하는 문제 → 여러 단계로 원인을 하나씩 배제해나갔지만, 실제로는 코드가 아니라 테스트 환경(VSCode 웹뷰) 문제였음. 다음부턴 브라우저 관련 버그가 안 풀리면 실제 테스트 환경(진짜 브라우저인지)부터 먼저 확인하기로 함
  - 실제 크롬에서도 오디오가 도중에 끊기는 문제 → 로그를 보니 같은 발언에 대한 생성 요청이 여러 번(최대 3번) 처음부터 다시 나가고 있었음. 영상 컴포넌트 자체 버그가 아니라, 그 앞 단계인 회의 분석 페이지(`ProjectDetailPage.jsx`)에서 분석 요청이 중복으로 나가던 기존 버그가 원인이었음
  - 위원 B 검증 중 같은 종류의 실수를 세 번 반복함 — `%%writefile app.py` 셀은 내용만 고친다고 반영되는 게 아니라 그 셀을 직접 다시 실행해야 코랩 디스크 파일이 바뀐다는 걸 계속 놓쳐서, 코드는 맞는데 서버는 옛날 버전으로 떠 있는 상황이 반복됨 — 코드 텍스트 확인뿐 아니라 `/health` 응답으로 서버가 실제로 뭘 들고 있는지 직접 확인하는 습관이 필요하다고 느낌
  - 우글거림 대응(색보정+스무딩) 실험이 `os.system()` 실패를 조용히 넘기는 옛날 버그가 있는 경로(8번 섹션)의 결과물에 의존하고 있어서, 애초에 제대로 된 입력으로 검증됐는지조차 불확실하다는 걸 재확인 — 게다가 그 경로 자체가 지금 실제 서비스가 쓰는 `app.py` 경로와 달라서 반영도 안 돼 있었음. 결과물 품질에 확신이 없다면 "실험이 제대로 돌았는지"부터 의심해봐야 한다는 교훈
- 다음 할 일 (우선순위 순 — 뒷 단계가 앞 단계 결과에 영향받을 수 있어 이 순서로 진행):
  1. 더 나은 TTS 후보(Zonos 등) 실제로 `musetalk_setup_v2.ipynb`(14번 섹션)에서 `app.py`의 TTS 호출 부분에 연결해보고 속도/음질 비교
  2. 말하는 속도랑 입모양이 시간이 지날수록 안 맞는다는 피드백 검증 (오디오-비디오 드리프트 의심 — 1·2번이 끝난 뒤 마지막에 확인)
  - 발언 내용 기반 `gesture_index` 자동 선택 로직 — 지금은 메커니즘만 있고 무엇을 기준으로 고를지는 미정, 어디서 계산할지 경이님과 논의 필요
  - 위원 A 우글거림/색보정을 지금 실제 서비스가 쓰는 경로(`app.py`의 `/generate`)로 새로 뽑은 결과물 기준으로 다시 검증 — 크롭 좌표 등 파라미터도 재확인
  - 위원 B(`persona_b`, technical_feasibility)를 실제 서비스가 쓰는 코랩 노트북/서버와 백엔드 `AVAILABLE_SPEAKER_IDS`에 반영 (지금까지는 별도 테스트 노트북/HTML로 검증만 완료, 실서비스 반영은 아직)
  - 위원 B 이음매·정지도 검증 (위원 A는 이미 확인, B는 아직)
  - Named Tunnel 전환 여부 팀과 논의 (도메인 비용 발생, 데모 단계엔 지금 화질 우회책으로 충분할 수도 있음)
  - `contracts/schemas/media_stream.schema.md` 윤한님 확인받기
  - 아바타 폴백 방식(무한루프 영상+TTS) 변경 건 팀 동의 구하기
  - `emotion` 필드 실제 계산 로직 마련 (현재 `"neutral"` 고정값)
  - 위원별 수동 트리거("버튼 형식") UX 검토
  - 경이님 계약 개정 제안(review_output v2.1.0) 회신 — schema_version "2.0.0" 하드코딩 지점 공유

## 2026-07-17

- 한 일:
  - `dev` 브랜치를 다시 pull — 팀 전체의 대규모 병합(RAG-003~007 근거 연동/게이팅, 유사사례 검색, HWP/HWPX 변환, 외부 시장·정책 자료 검색, 문서 업로드 화이트리스트 수정 등)을 받아옴. 로컬(`lji`)이 `origin/dev`와 완전히 동일한 상태인 것까지 확인
  - 병합으로 들어온 프론트 쪽 큰 변경(STEP1~7 위저드 화면 개편, `MentorFeedbackChatPage.jsx` 신규 등)을 직접 코드로 확인 — 특히 가은님이 오늘 새로 만든 STEP7 "대화형 피드백" 화면(`/ask` 엔드포인트, 질문 내용 보고 관련 위원 1~3명 자동 라우팅 + 병렬 응답)을 자세히 리뷰
  - 리뷰 중 발견: 지금 "위원이 말하는 방식"이 두 갈래로 나뉘어 있음 — ①최초 회의 결과(`media_script`)는 기존처럼 `CommitteeVideoStage`(재인님 파일)로 영상+음성 재생, ②STEP7 후속 질문 답변(`/ask` 응답)은 텍스트 말풍선(타이핑 애니메이션)으로만 표시되고 영상 파이프라인과 전혀 연결이 안 되어 있음
  - 후속 질문 답변도 위원 아바타 영상으로 말하게 만드는 연동 작업을 재인님이 맡기로 함 — 설계 논의: 지금 `CommitteeVideoStage`는 "받은 목록을 한 번에 처음부터 끝까지 재생"하는 구조라, 질문할 때마다 새 답변이 오면 재생 중이던 것까지 처음부터 다시 재생돼버리는 문제를 발견. "고정 목록 재생"이 아니라 "계속 쌓이는 대기열(큐)을 순서대로 소비"하는 구조로 바꾸고, 밖에서 새 줄을 추가할 수 있는 `enqueueLines()` 함수를 `forwardRef`/`useImperativeHandle`로 열어두는 방향으로 설계 확정
  - 이 연동을 위해 `MentorFeedbackChatPage.jsx`(가은님 파일)에도 `enqueueLines()` 호출 3줄 정도가 필요하다는 걸 확인 — 가은님께 무엇을, 왜 바꾸는지 설명하는 메시지 초안 작성(파일 소유자 확인 절차)
  - 백엔드 `AVAILABLE_SPEAKER_IDS`에 위원 B(`technical_feasibility`) 추가를 시도했다가, "코드 변경 전에 팀이 그동안 만들어둔 것부터 제대로 보자"는 판단으로 일단 되돌리고 팀 코드 리뷰를 먼저 진행함
  - TTS 대안(Zonos) 실제 연결 작업을 진행하다 라이브러리 자체 버그를 여러 개 만나서 오늘은 일단 보류하기로 함(자세한 진단 내용은 생략 — 다음에 재개 시 참고할 것)
- 결정/이유:
  - 위원 아바타 영상을 최초 리뷰뿐 아니라 STEP7 대화형 피드백에도 연동하기로 함 — 지금처럼 두 갈래(영상 vs 텍스트만)로 나뉜 채로 두면 사용자 경험이 일관되지 않는다고 판단
  - `CommitteeVideoStage`의 재생 로직을 "고정 목록"에서 "큐 소비"로 바꾸기로 함 — 새 발언이 계속 추가되는 대화형 상황에 맞는 구조는 이거밖에 없다고 판단(전면 재작성이 아니라 기존 `streamLine`/`fallbackLine`은 그대로 재사용하고 그걸 어떤 순서로 부르는지만 바꾸는 최소 변경으로 설계)
  - `MentorFeedbackChatPage.jsx` 변경은 가은님 확인 먼저 받고 진행하기로 함 — 팀 규칙(공용/타인 파일은 담당자 확인 후 수정) 그대로 적용
  - TTS 대안 작업은 오늘 마무리 못 지었지만 완전히 접은 건 아니고 "일단 보류, 나중에 재개"로 정리 — 위원 B 실서비스 연동이 지금 더 급한 작업이라고 판단해서 우선순위를 바꿈
- 다음 할 일:
  - `CommitteeVideoStage.jsx`를 큐 기반 구조로 리팩터링 + `enqueueLines()` 노출
  - `backend/app/api/routes/media.py`의 `AVAILABLE_SPEAKER_IDS`에 `technical_feasibility` 다시 추가
  - 가은님 승인 받으면 `MentorFeedbackChatPage.jsx`에 `enqueueLines()` 연결 코드 추가
  - 실제 코랩 서버(`musetalk_setup_v2.ipynb` 기준, 위원 A+B 등록된 상태)를 띄우고 `MEDIA_SERVICE_WS_URL`을 그 세션 주소로 맞춰서 엔드투엔드 확인
  - TTS 대안(Zonos) 작업 재개 여부/시점 판단
  - (2026-07-16에서 이어짐) 위원 B 이음매·정지도 검증, `gesture_index` 자동 선택 로직, Named Tunnel 전환, `emotion` 필드 계산 로직, `media_stream.schema.md` 윤한님 확인 등

## 2026-07-18

- 한 일:
  - `musetalk_setup_v2.ipynb`에서 검증 끝난 부분(위원 A/B 연동 + 제스처 영상 3개, Zonos 실험은 제외)만 추린 실행용 노트북 `musetalk_setup_launch.ipynb` 새로 만듦 — `app.py`의 Zonos 관련 코드(`ZONOS_SERVER_URL`, `tts_engine` 분기, `_generate_tts`의 zonos 브랜치)도 전부 제거하고 OpenAI 전용으로 되돌림
  - 이 노트북으로 실제 코랩 세션을 띄워서 처음으로 엔드투엔드 테스트 진행 — cloudflared Quick Tunnel URL을 `backend/.env`의 `MEDIA_SERVICE_WS_URL`에 반영. 처음엔 `/health`가 502로 응답해서 당황했지만, `uvicorn.log`를 직접 보고 "에러가 아니라 위원 2명 × 제스처 3개 = 총 6번의 페르소나 준비(프레임 추출→좌표 계산→VAE latent 계산→캐시 저장) 과정이 아직 안 끝난 것뿐"이라고 정확히 진단 — 대기 후 정상적으로 `Application startup complete` 확인, `/health`도 `personas_ready`에 6개 영상 다 잡힘
  - `musetalk_setup_launch.ipynb`의 "9. FastAPI 서버 백그라운드 실행" 섹션에 완전히 똑같은 셀이 2개 들어있는 걸 발견 — 원본 `v2.ipynb`에도 이미 있던 중복(예전 편집 실수 추정)이라 이번에 새로 생긴 문제는 아님, 지금 당장 문제를 일으키진 않아서 일단 기록만 해둠(다음에 정리 필요)
  - 실제 화면(STEP7 대화형 피드백)에서 테스트해보니 예상과 다른 동작 발견 — 진행 칩은 "기술·실현가능성 전문가 · 진행 중"인데 영상 영역은 "대기 중"으로 멈춰있었음. 원인을 짚어보니 실제로는 첫 번째 위원(창의성·독창성)의 립싱크가 아예 안 나오고 두 번째 위원(기술·실현가능성)만 나왔던 것 — 이 개별 버그는 아직 원인 미조사
  - 이 과정에서 애초에 사용자가 그리고 있던 그림과 지금 구현이 다르다는 걸 확인 — 사용자는 "위원 창 1개를 순서대로 돌려쓰기"가 아니라 "위원마다 각자 전용 창이 있고, 그 창이 각자 자기 대기 루프를 돌리다가 자기 차례(요청 응답)가 오면 그 창에만 립싱크 영상이 얹히는" 그림이었음. 요청 자체는 여전히 한 번에 하나씩 순서대로 보낸다는 점은 동일 — 화면에 동시에 여러 창이 떠 있다는 것만 다름
  - 확인 질문으로 세부 사항 확정: 전용 창 개수는 "등록된 아바타 수만큼 고정"(지금 2개, `AVAILABLE_SPEAKER_IDS`처럼 프론트에도 목록 하나 더 관리), 아바타 영상이 없는 위원(위원장 등)은 창 자체를 안 만들고 텍스트만 아래 채팅으로 보이면 됨(가은님이 만든 "정적 이미지 대체" 폴백 연출은 제거)
  - `CommitteeVideoStage.jsx`를 위원별 전용 창 구조로 재작성 — 위원마다 `<video>`/idle 루프 경로를 따로 관리(`AVATAR_TILES` 상수), 등록 안 된 위원 줄은 영상 큐 처리 자체에서 건너뛰고 자막만 읽을 시간만큼 보여줌. 프론트에 없던 위원 B(`technical_feasibility`) 대기 루프 영상 자산(`avata_b_rf.mp4`)이 `frontend/public/mock-videos/`에 아예 없다는 것도 이번에 발견해서 `ai/media/assets/persona_b/`에서 복사해 채워넣음
  - STEP3(공모전 분석) 진입 시 "Failed to fetch"만 뜨는 문제 발생 — 백엔드 로그를 직접 봐서 진짜 원인 확인: `openai.RateLimitError: insufficient_quota`(재인님 개인 OpenAI 키 크레딧 소진). CORS 헤더 없이 500이 나가면서 브라우저가 이걸 네트워크 에러("Failed to fetch")로 보여준 것 — 코드 문제가 아니라 결제 문제였음. 카드 결제도 첫 해외결제라 토스뱅크에서 하루 임시 차단해둔 상태였던 것까지 확인
  - (다음날) 가은님한테 임시 API 키 받아서 `backend/.env` 교체, 코랩도 `musetalk_setup_launch.ipynb`로 다시 실행 → 새 cloudflared URL로 `MEDIA_SERVICE_WS_URL` 갱신, `/health` 정상 확인
  - 백엔드/코랩 다시 살아난 상태로 재테스트 — 아바타 A(당시 "창의성·독창성 전문가") 여전히 영상도 "발언 중" 표시도 안 뜸, B(기술·실현가능성)만 정상. 백엔드 로그를 보니 `/media/stream` 웹소켓 연결이 `technical_feasibility` 하나만 열렸고 `business_strategy` 요청 자체가 안 나간 상태 — 브라우저 콘솔로 실제 `media_script`를 까보니 이번 회의 위원 구성이 `['creativity_originality', 'technical_feasibility']`였음(위원 구성은 문서 종류마다 LLM이 다르게 뽑음). 즉 코드 버그가 아니라 "아바타 창을 `business_strategy`라는 특정 persona_id에 고정해놨는데, 이번 회의엔 그 위원이 아예 없어서" 생긴 당연한 결과였음
  - 아바타 슬롯을 특정 persona_id 고정에서 "이번 회의에서 순서상 1번째·2번째로 등장하는 위원"으로 바꿔 재설계 — 화면 표시(이름/색/자막)는 실제 위원(`speaker_id`) 그대로 쓰고, 코랩에 보내는 WebSocket 요청의 `speaker_id`만 슬롯에 등록된 코랩 ID(`business_strategy`/`technical_feasibility` — 코랩 `PERSONA_MAP`에 실제로 얼굴·목소리가 등록된 유일한 ID들)로 바꿔치기하도록 수정. 실제 코랩 세션으로 재테스트해서 정상 작동 확인
- 결정/이유:
  - 아바타 영상 표시 방식을 "공유 창 1개, 순서대로 재생"에서 "위원별 전용 창, 각자 독립적으로 대기/재생"으로 변경 — 사용자가 처음부터 그리고 있던 그림이 이거였고, 실제 화면으로 보니 공유 창 방식은 "지금 누가 말하는지"가 직관적으로 안 와닿는다고 판단
  - 영상 없는 위원의 폴백 연출(정적 이미지 대체 오버레이, 가은님이 2026-07-16에 추가)을 제거하기로 함 — 위원별 전용 창 구조에서는 "그 위원의 창"이라는 개념 자체가 명확해서, 창이 없는 위원은 오히려 아무것도 안 보여주는 게(텍스트는 아래 채팅에 항상 있으므로) 더 일관적이라고 판단. 재인님 파일이라 가은님과 공유는 별도로 필요하지만 코드 변경 자체는 파일 소유자 권한으로 바로 진행
  - 전용 창 개수는 회의마다 동적으로 정하지 않고 "프론트에 등록된 아바타 자산 수만큼 고정"(지금 2개)으로 결정 — 나중에 아바타 자산이 늘어나면 코드 상수(`AVATAR_SLOTS`)를 사람이 직접 늘리는 방식, 백엔드 `AVAILABLE_SPEAKER_IDS`와 마찬가지로 수동 동기화 대상
  - 아바타 슬롯을 특정 persona_id에 고정하지 않고 "회의 내 등장 순서"로 배정하기로 함 — 아바타 얼굴·목소리 자산(2개)은 한정돼 있는데 위원 구성(누가 몇 명 뽑히는지)은 문서 종류마다 달라지는 이 프로젝트 특성상, 특정 역할에 고정하면 그 역할이 이번 회의에 없을 때마다 창이 계속 비는 문제가 반복될 수밖에 없다고 판단. 코랩 쪽 얼굴·목소리는 "역할을 연기하는 배우"처럼 다루고, 실제 위원 정체성(이름/색/자막)은 화면 표시에서만 정확하게 유지
- 막힌 점:
  - 처음 코랩 서버가 502를 반환해서 당황했지만 실제로는 정상적인 모델 로딩+캐싱 시간(총 6번의 페르소나 준비, 한 번에 20~60초)이었던 것 — 로그(`uvicorn.log`)를 실제로 tail해서 확인하지 않았으면 "서버가 죽었다"고 오판할 뻔함. 코랩 기반 서버는 시작 직후 몇 분간 502가 나는 게 정상일 수 있다는 걸 기억해둘 것
  - OpenAI 크레딧 소진으로 STEP3 분석 자체가 하루 정도 막힘 — 개인 키에 의존하는 로컬 개발 환경의 리스크를 실감. 급한 프론트 확인(아바타 무한루프 등)은 `sessionStorage`에 목업 `media_script`를 직접 넣어서 OpenAI/코랩 없이도 확인 가능하다는 걸 활용
  - `musetalk_setup_launch.ipynb`의 9번 섹션 중복 셀(서버 백그라운드 실행) 정리 완료
  - TTS 대안 재조사 — Zonos-v0.1(예전에 시도했던 버전)이 사실 한국어 공식 지원 언어가 아니었다는 걸 발견(공식 5개 언어: 영/일/중/불/독). 2026-06 출시된 신버전 ZONOS2는 한국어를 Tier 2로 공식 지원 + 바이트 토큰화로 비유럽권 언어 처리 개선. VRAM 요구량은 오히려 늘어남(6GB→16GB 권장) — 지금 코랩이 A100 80GB라 여유는 충분히 확인
  - MuseTalk 없이 ZONOS2만 단독으로 목소리+속도 확인하는 새 노트북(`ai/media/musetalk/colab/zonos2_tts_only_test.ipynb`) 제작해서 실측 — 첫 호출만 워밍업(CUDA 그래프 캡처 등)으로 46초까지 튀고, 그 이후론 정상 속도(문장 길수록 오히려 빠름, 143~150 frames/s). 다만 한국어 텍스트 정규화가 첫 호출에서 실패해서 원문 그대로 나간 로그도 발견 — 발음 정확도에 영향 줄 수 있는 부분
  - "TTS 시간 줄이려다 오히려 늘어나는 것 같다"는 판단으로 Zonos 계열 탐색을 잠정 중단하고 ElevenLabs로 방향 전환 — Creator 플랜($22, 첫달 $11) 결제, API 키 발급(Voice Library 접근에 최소 권한: 텍스트음성변환+음성 읽기만), 위원 A/B용 한국어 보이스 선택(Kelee K - Seoul Narrator / Yohan Koo - Encouraging, Clear and Airy)
  - 로컬에서 순수 TTS만 비교하는 스크립트(`ai/media/tts/elevenlabs_compare_test.py`, `ai/media/tts/openai_out/`) 작성 — OpenAI(2~3초) 대비 ElevenLabs multilingual_v2가 1.5~2배, flash_v2_5가 4~7.5배 빠름을 실측 확인. 여기까지는 MuseTalk 없이 순수 TTS 생성시간+파일만 비교
  - 실제 립싱크까지 붙여서 확인하기 위해 별도 실험용 노트북(`ai/media/musetalk/colab/musetalk_setup_elevenlabs_test.ipynb`, `musetalk_setup_launch.ipynb`를 복사해서 `_generate_tts()`만 ElevenLabs REST 호출로 교체) 제작, `web_test_chat_two_avatars.html`로 실제 테스트 — 목소리 품질 자체는 만족스러웠으나 flash_v2_5는 음질이 조금씩 깨진다는 피드백으로 multilingual_v2로 최종 확정
  - 실제 테스트 중 버퍼링(끊김) 문제 발견 — 서버 로그(`[SEND-TIMING]`의 `wait=`)와 클라이언트 디버그 로그(`버퍼 부족` 타임스탬프)를 시간 맞춰 대조해서, "발화 끝나고 무음 tail(원본 루프 영상 그대로 이어붙이는 부분, GPU 연산 없음)을 발화 프레임과 똑같이 실시간 배속으로 천천히 흘려보내던 게" 원인이라고 정확히 진단 — 특히 짧은 문장일수록 tail이 루프 전체 길이(최대 241프레임)까지 길게 붙어서 더 잘 걸림
  - `app.py`의 `_pace_and_feed()`를 고쳐서 tail 프레임은 페이싱(sleep) 없이 즉시 흘려보내도록 수정(`musetalk_setup_elevenlabs_test.ipynb`, `musetalk_setup_launch.ipynb` 둘 다 반영) — 실측으로 요청 전체 소요가 8~24초에서 3.7~3.8초로 크게 줄어듦을 확인
  - 그런데 서버가 데이터를 "꾸준히 조금씩"이 아니라 "몰아서 한 번에" 보내는 패턴으로 바뀌면서, 클라이언트의 재생 시작 기준(`RESUME_THRESHOLD=0.8`)이 너무 성급해져서 되레 다른 지점에서 버퍼링이 남는 걸 확인 — `CommitteeVideoStage.jsx`/`web_test_chat_two_avatars.html`/`web_test_chat.html` 세 파일 모두 `RESUME_THRESHOLD`를 1.5로 상향
  - 근데 짧은 문장("안녕하세요")에서는 재생 시작 기준을 올린 부작용으로 "말하기 전부터 화면이 멈춘 것처럼" 보이는 새 증상 발견 — 알고보니 크롬이 "전력 절약을 위해 배경 영상 재생을 중단시켰다"며 대기 루프 `video.play()` 재호출을 거부하는 사례도 실제 로그에서 확인(한 번은 147초간 안 풀림). 세 증상(중간 버퍼링/크롬 재생 거부/버퍼링 중 앞쪽 멈칫)의 근본 원인이 전부 "대기 루프와 스트리밍용으로 `<video>` 태그 하나를 계속 껐다 켰다 하는 구조"라는 걸 파악
  - `CommitteeVideoStage.jsx`, `web_test_chat_two_avatars.html`을 대기 루프 전용/스트리밍 전용 `<video>` 2개로 분리하는 구조로 재작성 — 대기 루프는 마운트 시 딱 한 번 재생 시작하면 이후 절대 재호출 안 하고 계속 뒤에서 돌고, 스트리밍 엘리먼트는 평소 투명하게 숨겨뒀다가 버퍼가 충분히 쌓여 실제 재생이 걸렸을 때만 위로 드러나는 방식(opacity 전환). 실제 코랩 세션(짧은 문장 포함)으로 재테스트해서 개선 확인
  - TTS를 ElevenLabs(multilingual_v2)로 최종 확정. 다음 작업으로 "아바타 2명 → 4명 확장" 논의 중, 위원 후보 풀을 실제로 확인해보니 4명이 아니라 8명(위원장 제외)이고, 공통 2명(business_strategy/technical_feasibility) 외 나머지 2자리는 도메인(스타트업형/정부지원사업형/공모전형)마다 완전히 다른 위원이 배정된다는 걸 `ai/meeting/personas/{persona_cards,startup,government_support,competition}.json`으로 확인. 대신 도메인마다 항상 정확히 4명이 나온다는 것도 확인 — "특정 persona_id 4개 고정"이 아니라 지금 있는 "순서 기반 동적 배정"(먼저 말하는 순서대로 슬롯 배정)을 2슬롯→4슬롯으로 그대로 확장하면 어느 도메인이든 항상 4명 다 커버된다는 결론으로 정리
- 결정/이유(2026-07-18 후반):
  - TTS는 ElevenLabs(model: `eleven_multilingual_v2`)로 최종 채택, OpenAI 대비 속도 우위 + 품질 만족 — `eleven_flash_v2_5`는 더 빠르지만 음질 저하(음성이 조금씩 깨짐)로 기각. 유료 서비스(월 $22)로 전환하는 결정이라 `tts_engine_decision.md`에 반드시 반영 필요(다음 할 일)
  - 아바타 4명으로 확장할 때도 "특정 위원에 얼굴 고정"이 아니라 지금의 "회의 내 등장 순서로 슬롯 배정" 방식을 그대로 유지하기로 함 — 도메인마다 항상 정확히 4명이 나온다는 사실을 확인했기 때문에, 순서 기반이면 슬롯 개수만 늘려도 어느 도메인이든 항상 완전히 커버됨(도메인별로 다른 매핑 로직을 따로 안 짜도 됨)
- 막힌 점(2026-07-18 후반):
  - "TTS 속도 개선"이 목적이었는데, 실제로 재보니 진짜 병목은 TTS가 아니라 MuseTalk 프레임 생성 자체(전체 소요의 80%+)였다는 걸 뒤늦게 확인 — TTS 엔진 교체만으로는 체감 속도가 크게 안 바뀔 수 있다는 걸 실측으로 배움. 대신 버퍼링/끊김 문제(tail 페이싱, 크롬 전력절약 정책)는 이번에 제대로 잡아서 실질적인 개선이 있었음
  - 버퍼링 원인을 처음엔 "서버가 느려서"로 짐작했다가, 서버/클라이언트 로그를 시간 맞춰 직접 대조해보고 나서야 "tail을 실시간 배속으로 천천히 보내는 설계 자체"가 원인임을 알게 됨 — 짐작으로 고치지 않고 실측 로그 두 개를 나란히 놓고 비교한 게 정확한 진단의 핵심이었음
- 다음 할 일:
  - 아바타 2명 → 4명 확장 — 새 아바타 2명분 영상(대기 루프+제스처) 촬영/생성 필요, 이후 `AVATAR_SLOTS`(프론트)와 코랩 `PERSONA_MAP`/`AVAILABLE_SPEAKER_IDS`(백엔드)에 반영
  - TTS 최종 결정(ElevenLabs) `ai/media/tts/tts_engine_decision.md`에 정식 반영, `musetalk_setup_launch.ipynb`의 `_generate_tts()`도 OpenAI에서 ElevenLabs로 정식 교체(지금은 실험용 노트북에만 있음)
  - TTS 교체(Zonos/ZONOS2) — 이번에 ElevenLabs로 방향이 정해졌으니 계속 보류할지 완전히 접을지 판단
  - (2026-07-17에서 이어짐) 위원 B 이음매·정지도 검증, `gesture_index` 자동 선택 로직, Named Tunnel 전환, `emotion` 필드 계산 로직, `media_stream.schema.md` 윤한님 확인 등

## 2026-07-19

- 한 일:
  - 아바타 4명 확장을 실제로 반영 — `frontend/public/mock-videos/`에 persona_c/d 대기 루프 영상 복사, `CommitteeVideoStage.jsx`의 `AVATAR_SLOTS`와 `backend/app/api/routes/media.py`의 `AVAILABLE_SPEAKER_IDS`를 4개로 확장(순서 기반 동적 슬롯 배정 방식은 그대로 유지)
  - 실행용 노트북을 `musetalk_setup_launch_v2.ipynb`(신규)로 새로 만듦 — OpenAI 관련 코드 완전히 제거하고 ElevenLabs(`eleven_multilingual_v2`)를 정식 반영, `PERSONA_MAP`에 4명(business_strategy/technical_feasibility/persona_c/persona_d) 전부 등록. persona_c/d는 아직 대기 루프 영상만 있어 제스처 영상 자리는 비워둠
  - `tts_engine_decision.md`(ElevenLabs 최종 결정 반영), `voice_map.json`(4명 보이스 매핑: Kelee K/Theo/Gihong/Han)을 정식 문서로 업데이트
  - 노트북에 로그 확인용 셀(서버 기동 로그, 요청 처리 로그, 실시간 tail -f)을 단계별로 미리 삽입해달라는 요청 반영
  - `git pull origin dev` 진행(재인님이 직접 실행, Claude는 명령어만 제공) — 팀 대규모 변경 정상 병합 확인. `*.ipynb`/`ai/media/musetalk/*.html`을 `.gitignore`에 추가(이미 추적 중이던 `musetalk_setup.ipynb`는 그대로 추적 유지하기로 확인 후 예외 처리)
  - 4명 아바타로 실제 코랩 세션 라이브 테스트 성공 확인
  - "위원 A 발언 끝나고 B로 넘어가는 사이가 너무 오래 걸린다"는 문제로 프론트 파이프라이닝 최적화 진행 — 위원별 요청/토큰/타이머를 전부 개별 관리하도록 바꾸고, 다음 위원 요청을 "화면에 뜨자마자" 미리 보내 백그라운드에서 준비시키되 화면 전환(reveal)은 이전 위원이 완전히 끝날 때까지 보류하는 구조로 1차 개선
  - 실측 중 "립싱크(실제 발화)는 짧고 무음 tail이 긴 경우, 다음 위원이 이미 준비됐는데도 의미 없는 tail을 계속 기다리는" 낭비를 발견 — tail을 강제로 끊는 방안을 검토하다, tail을 만든 원래 이유(예전 "영상 태그 하나 재사용" 구조에서 발화→대기루프 전환 시 프레임이 안 맞아 화면이 튀는 걸 방지)가 지금의 "대기 루프/스트림 영상 분리" 구조에서는 이미 의미가 없어졌다는 걸 코드 주석을 다시 읽고 확인. tail은 무음(GPU 연산도 없음)이라 화면에 남아 있어도 다음 위원 오디오와 겹칠 소리가 없다는 점도 같이 확인
  - 논의 끝에 "tail 자체는 그대로 두되, 코랩이 TTS(실제 발화) 종료 시점을 신호로 알려주면 그 시점에 '발언 중' 배지만 끄고, +2초 뒤(또는 그 전에 자연 종료되면 그 즉시 — 둘 중 먼저 오는 조건) 다음 위원이 화면을 넘겨받는다"는 방향으로 설계 확정 — 강제 pause/mute 없이 자연 재생 유지
  - 코랩(`_pace_and_feed()`)에 tail 시작 시점을 프론트에 알려주는 `tts_end` WebSocket 신호 추가, 프론트(`CommitteeVideoStage.jsx`)에 위원별 전환 게이트(`transitionGateRef`)와 배지·화면노출 상태 분리(`speakingMap`/`revealedMap`) 구현
  - 실제 라이브 테스트 중 버그 발견: 아직 발화 중인데 배지가 먼저 꺼지고 다음 위원으로 넘어감 — 원인 진단: `tts_end` 신호는 "서버가 도착시킨 시점" 기준인데, 데이터는 항상 재생 위치보다 앞서 도착하는 데다 tail은 페이싱 없이 몰아서 보내다 보니 신호 도착 시점과 실제 화면 재생 위치 사이에 큰 시차가 있었음 — 코랩이 "몇 초 지점부터 tail인지"(`speech_seconds`)를 같이 보내도록 바꾸고, 프론트는 신호 도착 즉시가 아니라 `video.currentTime`이 그 지점을 실제로 지날 때만 반영하도록 수정
  - 두 번째 버그 발견: 위원 4명 실측에서 "C가 아직 발언 중(배지 안 꺼짐)인데 D가 먼저 발언 시작"하는 겹침 현상 — 원인 진단: 전환 게이트가 "누가 지금 게이트를 쥐고 있는지" 구분 없이 아무나 열 수 있게 돼 있어서, tail이 길어 뒤늦게 자연 종료되는 A의 정리 로직이 이미 C가 쥐고 있는 게이트를 멋대로 열어버림 — 게이트에 소유자(owner) 필드를 추가해 "지금 게이트를 쥔 위원 본인만 열고 닫을 수 있게" 수정
  - 재테스트로 두 버그 모두 해결 확인
  - 화면에 뜬 문서 URL 가져오기 관련 "확인 필요" 경고가 재인님 작업과 무관한 팀원(RAG 담당) 신규 기능(공고문 URL 수집 강화 커밋)이며 로컬에 Playwright 미설치로 뜨는 정상 안내라는 걸 git log로 확인해드림
  - 위원 B TTS 보이스를 Theo에서 Harry(Inciting, Soft and Steady)로 재교체 — `voice_map.json`/`tts_engine_decision.md`/노트북 `PERSONA_MAP` 세 곳에 반영
  - 위원 C·D 제스처 영상용 이미지-투-비디오 프롬프트 여러 차례 다듬음 — D는 마음에 안 드는 기존 인물이라 새 사진으로 rf(대기 루프)부터 다시 뽑음. "머리 완전 고정" 지시가 호흡 등 자연스러운 움직임까지 같이 죽이는 문제(A/B 만들 때도 겪었던 것과 동일)와, "설명하는/발표하는" 프레이밍이 입을 계속 움직이게 만들어(MuseTalk이 입 부분을 통째로 덮어씌우므로 오히려 방해가 됨) "말 안 하고 제스처만 하는 상황"으로 재구성해 해결
  - persona_c/d 제스처 영상(`_1`/`_2`) 2개씩 + D 새 rf 영상 실제 파일 반영 — `ai/media/assets/`와 `frontend/public/mock-videos/`(D rf만 해당) 동기화 확인, 노트북 `PERSONA_MAP`의 `video_paths`에 전부 등록(4명 전원 rf+제스처 2개 = 3개씩)
  - 위원별 발화마다 자세가 다양하게 나오도록 `gesture_index` 로테이션 구현(`CommitteeVideoStage.jsx`) — 직전에 그 위원이 썼던 인덱스만 제외하고 나머지 중 랜덤 선택, 백엔드(`media.py`)는 그대로 중계만 하므로 수정 불필요(낡은 주석만 최신화)
  - 아바타 파이프라인 전체(프론트 AVATAR_SLOTS ↔ 백엔드 AVAILABLE_SPEAKER_IDS ↔ 코랩 PERSONA_MAP ↔ voice_map.json ↔ 실제 파일)를 처음부터 끝까지 대조 점검 — 전부 일치 확인, `media.py` 상단 주석이 "제스처 영상 아직 없음"으로 낡아있던 것만 발견해 최신화
  - 실사용 재현 중 "위원이 발화 도중에 끊기고 다음 위원이 끼어드는" 심각한 버그 재발견(1차 회의·채팅 답변 둘 다에서 발생) — 처음엔 어제 고친 전환 게이트 로직 문제로 의심했으나, 게이트/owner/reveal 시점마다 타임스탬프를 찍는 진단 로그(`gateLog`, 임시로 남겨둠)를 추가해 실측해보니 게이트 로직 자체는 설계대로 완벽하게 동작 중이었음 — 실제 원인은 완전히 다른 층: 백엔드↔코랩 WebSocket 연결이 keepalive 핑퐁 타임아웃(`no close frame received or sent`)으로 죽는 것이었음. 코랩이 위원 여러 명 요청을 동시에 처리하느라 바빠지면(GIL 경합) 핑에 제때 응답을 못 해 "바쁜 것"을 "죽은 것"으로 오판해 연결이 끊기고, 이게 그 위원의 finish()를 error 경로로 강제 호출해 다음 위원에게 게이트가 넘어가는 식으로 보였던 것
  - 1차 조치로 `backend/app/api/routes/media.py`의 `websockets.connect()`에 `ping_interval=None` 적용(백엔드→코랩 방향 핑 자체를 끔) — 재테스트로 크게 개선됐으나 한 번 더 다른 위원에서 같은 종류 에러 재현
  - 재진단해보니 핑퐁이 양방향으로 따로 있었다는 걸 확인 — 코랩 서버(uvicorn) 자체도 자기 쪽에서 백엔드로 핑을 보내고 있었고, 이건 안 꺼져 있었음. 노트북 9번 셀의 uvicorn 실행 명령에 `--ws-ping-interval`/`--ws-ping-timeout`을 86400(사실상 비활성화)으로 추가해 서버 쪽 핑퐁도 마저 제거
  - 최종 재테스트: 4명 회의 + 3명 채팅 답변(최대 59.7초짜리 발화 포함) 전부 연결 끊김 없이 정상 완료 확인
  - stream(립싱크)에서 idle(무한루프)로 돌아갈 때 자세가 툭 튀어 보이는 이음매 문제 수정 — idle이 마운트 이후 한 번도 안 건드려지고 독립적으로 계속 돌고 있어서, stream이 끝나는 순간 idle이 몇 프레임째인지가 우연에 달려있던 게 원인. `finish()` 시점에 idle을 0번 프레임으로 seek하되, `seeked` 이벤트(또는 300ms 타임아웃 안전장치)를 기다린 뒤에야 화면을 전환하도록 수정 — 그동안 stream은 이미 멈춘 마지막 프레임을 계속 보여주고 있어서 화면이 비는 순간이 없음. 실측으로 깜빡임/검은 화면 없음 확인
- 결정/이유:
  - 위원 화면 전환을 "직전 위원 완전 종료까지 대기"에서 "TTS 실제 발화 종료 + 최소 2초 유예(또는 그 전 자연 종료 시 즉시)"로 변경 — 0초 즉시 전환은 말을 끊는 것처럼 보이고, tail을 강제로 자르는 방식은 원본 tail의 존재 의미(무음 자연 재생)를 해치므로, "발화는 자연 재생, 전환 판단만 발화 종료 시점 기준"으로 절충. 2초 값은 사용자가 "회의에서 한 사람 말 끝나고 짧은 정적 후 다음 사람"이 오히려 자연스럽다고 판단해 확정
  - "화면에 한 번에 한 명만 노출"이라는 기존 제약을 없애고, "배지(발언 중 표시)만 한 번에 한 명"으로 완화 — 위원마다 타일이 이미 분리돼 있어 여러 명이 동시에 자기 stream 영상을 보여줘도(한쪽은 무음 tail, 한쪽은 발화) 기술적으로 문제없다는 걸 확인했기 때문
  - 전환 게이트에 소유자 개념을 추가하기로 함 — 공유 상태를 여러 비동기 흐름이 동시에 건드릴 수 있는 구조에서는 "내가 이걸 바꿔도 되는 주인인지" 확인이 필요하다는 걸 이번 버그로 재확인
  - WebSocket 핑퐁 문제는 "타임아웃 값을 늘리는" 임시방편 대신 "핑퐁 자체를 없애는" 근본 해결로 감 — 실제 데이터(영상 프레임/status/tts_end/done 메시지)가 계속 오는지로 연결 상태를 판단할 수 있어 별도의 생존 확인이 필요 없다고 판단. 값만 늘리면 발화가 그 값보다 더 길어질 때 다시 재발할 걸로 예상됨
  - Claude가 설명만 하고 승인 없이 바로 코드를 고친 것에 대해 피드백 받음 — 이후로는 진단/제안까지만 먼저 드리고, "고쳐"/"ㄱㄱ" 같은 명확한 승인을 받은 뒤에만 실제 코드 변경 진행하기로 함
- 막힌 점:
  - `tts_end` 신호 타이밍 버그 — 처음엔 "신호를 받으면 바로 반영"이 당연하다고 가정했는데, 스트리밍 특성상 데이터 도착과 실제 재생 위치 사이에 시차가 있다는 걸 놓쳤음. 사용자가 "TTS 끝나기 전인데 배지가 꺼진다"고 정확히 관찰해준 덕분에 원인을 다시 짚을 수 있었음 — 신호는 항상 "언제 왔는지"가 아니라 "무엇을 가리키는지"로 받아서 재생 위치와 대조해야 한다는 교훈
  - 게이트 겹침 버그 — 위원 2명일 때는 절대 안 드러나고 4명으로 늘려야만 재현되는 경합 문제였음(먼저 시작한 위원의 뒤늦은 종료 처리가 이미 여러 단계 진행된 뒷사람의 상태를 덮어씀). 사용자가 "C 배지 안 꺼졌는데 D가 떴다"는 구체적인 증상을 짚어줘서 바로 원인 특정 가능했음 — 공유 가변 상태는 인원이 적을 때 테스트가 통과해도 안심할 수 없다는 걸 재확인
  - "겹침" 버그를 처음엔 프론트 게이트 로직 문제로 확신하고 파고들었으나, 진단 로그를 붙여 실측해보니 그 로직 자체는 완벽하게 설계대로 동작 중이었음 — 진짜 원인은 완전히 다른 층(백엔드-코랩 WebSocket keepalive)에 있었고, 코드만 계속 들여다봐선 못 잡았을 문제라 실제 에러 로그(`keepalive ping timeout`/`no close frame received or sent`)를 정면으로 본 게 결정적이었음
  - 핑퐁 문제가 양방향(백엔드→코랩, 코랩→백엔드)으로 각각 따로 있다는 걸 처음엔 몰라서, 1차 수정(백엔드 쪽만) 후에도 문제가 한 번 더 재현됨 — 재현·재테스트로 남은 절반(코랩 서버 쪽)을 마저 찾아 고침
- 다음 할 일:
  - 립싱크 디테일 폴리싱 (목표: 다음주 목요일, 2026-07-23) — 이번 주말~다음 주 초반 진행
  - 임시로 남겨둔 진단 로그(`gateLog`, `[CVS:gate]`) 계속 지켜보다가 안정성 충분히 확인되면 정리
  - (이월) 오디오-비디오 드리프트 검증, Named Tunnel 전환 논의, `emotion` 필드 계산 로직, `media_stream.schema.md` 윤한님 확인, `gesture_index` 발언 내용 기반 자동 선택 로직(지금은 "직전 것 제외 랜덤"까지만 구현됨)

## 2026-07-20

- 한 일:
  - 위원 A 제스처 영상(`avata_2.mp4`)을 실제로 프레임 단위로 열어서 확인 — 갑자기 없던 펜이 나타나 시선을 아래로 깔고 필기하는 동작이 있었음. 원인은 "필기 동작이 시선을 카메라에서 벗어나게 해서 안 쓰기로" 했던 예전 결정(2026-07-16)이 반영되기 전에 만들어진 영상이었기 때문 — 최신 방식(펜/소품 없이 손짓만, 시선 카메라 고정)으로 다시 뽑기로 하고 프롬프트 여러 차례 다듬음(손동작이 너무 적다는 피드백으로 "계속 작게 움직이는 리듬" 추가, 입 고정 지시를 별도 문단으로 분리해서 강하게 재작성)
- 아바타 해야할 일:
  - A `avata_2.mp4` 새 프롬프트로 재생성 후 결과 확인(손동작 자연스러운지, 입 안 움직이는지, 펜/소품 없는지)
  - 나머지 A/B/C/D 영상들도 이런 문제(펜·소품, 원치 않는 입 움직임) 없는지 한 번씩 프레임 단위로 점검
  - 오디오-비디오 드리프트(말하는 속도랑 입모양이 시간 지날수록 안 맞는다는 피드백) 검증
  - `gesture_index` 발언 내용 기반 자동 선택 로직 — 지금은 "직전 것 제외 랜덤"까지만 구현됨
  - `emotion` 필드 실제 계산 로직 (지금은 `"neutral"` 고정값, 생성되는 영상 표정/톤에 영향)
